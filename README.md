# NYCanvas
A struct for a 2D array of pixels, representing an image. Currently uint16_t pixel format (RGB 565), because that's the fb pixel format on my pi. Some canvas functions are also included, like image loading, text drawing, and simple drawing.
